{
  "entities": {
    "ImageConfig": {
      "title": "Image Configuration",
      "description": "Dynamic image settings for the website",
      "type": "object",
      "properties": {
        "key": { "type": "string", "description": "Unique identifier for the image location (e.g. 'home_hero')" },
        "url": { "type": "string", "description": "The URL of the uploaded image" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "updatedBy": { "type": "string" }
      },
      "required": ["key", "url"]
    }
  },
  "firestore": {
    "/images/{imageId}": {
      "schema": "ImageConfig",
      "description": "Collection of dynamic images managed by the admin"
    }
  }
}
